#!/bin/bash
#
# Конфигурация ИИ-агента
# Измени эти значения, чтобы настроить поведение агента.
#

# Как часто cron запускает агента (в минутах).
# Значение должно совпадать с записью в crontab.
SESSION_INTERVAL_MINUTES=10

# Максимальная длительность сессии в секундах.
# Если сессия идет дольше, она будет остановлена.
SESSION_TIMEOUT_SECONDS=1800  # 30 минут

# Частота консолидации памяти (каждые N сессий).
# По умолчанию: 5 (сессии 5, 10, 15 и т.д.).
# CONSOLIDATION_INTERVAL=5  # Пока не реализовано; при необходимости меняется в run_ai.sh.

# Имя OpenAI-compatible модели для ./run_ai.sh compatible.
# API key и base URL настраиваются через ./setup-compatible.sh.
# COMPATIBLE_MODEL="gpt-4o-mini"
